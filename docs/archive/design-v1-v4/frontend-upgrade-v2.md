# RealWorldClaw 前端企业级升级方案 v2

> 🌸 花羊羊 CPO 出品 | 2026-02-21
>
> 设计哲学：**温暖 · 克制 · 可触 · 有趣**
>
> 对标：Vercel Dashboard · GitHub · OpenClaw Dashboard

---

## 目录

1. [当前状态分析](#1-当前状态分析)
2. [页面架构规划](#2-页面架构规划)
3. [设计系统](#3-设计系统)
4. [实时功能设计](#4-实时功能设计)
5. [技术升级方案](#5-技术升级方案)
6. [页面线框图描述](#6-页面线框图描述)
7. [实施路线图](#7-实施路线图)

---

## 1. 当前状态分析

| 维度 | 现状 | 目标 |
|------|------|------|
| 框架 | Next.js 14 + Tailwind CSS | Next.js 14 + shadcn/ui + Zustand |
| 页面数 | ~12 基础展示页 | 7 大模块 30+ 子页面 |
| 状态管理 | 无 | Zustand |
| 数据获取 | 基础 fetch | SWR / React Query |
| 实时通信 | 无 | WebSocket |
| 动效 | 无 | Framer Motion |
| 设计系统 | 零散 Tailwind | 完整 Design Token + 组件库 |

---

## 2. 页面架构规划

### 路由结构

```
/                           → Landing（未登录）
/dashboard                  → Dashboard 首页
/devices                    → 设备管理
/devices/[id]               → 设备详情（实时状态）
/marketplace                → 模块商城
/marketplace/[id]           → 模块详情（3D预览）
/orders                     → 订单管理
/orders/new                 → 创建订单
/orders/[id]                → 订单详情
/maker                      → Maker 中心
/maker/earnings             → 收入统计
/maker/orders               → 接单管理
/studio                     → 设计工作台
/studio/upload              → 上传 STL
/studio/[id]                → 编辑/预览设计
/settings                   → 设置
/settings/profile           → 个人信息
/settings/api-keys          → API Keys
/settings/notifications     → 通知偏好
```

### 布局系统

```
AppShell
├── Sidebar（左侧，可折叠，宽 240px / 折叠 64px）
│   ├── Logo
│   ├── Nav Items（Dashboard / 设备 / 商城 / 订单 / Maker / 工作台）
│   ├── --- 分隔线 ---
│   └── Settings / User Avatar
├── TopBar（右侧顶部）
│   ├── Breadcrumb
│   ├── Search（Cmd+K 全局搜索）
│   ├── Notifications Bell
│   └── User Menu
└── Main Content Area
```

**响应式策略：**
- Desktop (≥1280px): Sidebar 展开 + 主内容区
- Tablet (768-1279px): Sidebar 折叠为图标 + 主内容区
- Mobile (<768px): 底部 Tab Bar + 全屏内容

---

## 3. 设计系统

### 3.1 调色板

以暗色主题为主，提供亮色切换。参考 Vercel/GitHub Dark，加入 RealWorldClaw 品牌温度。

```
── 背景层 ──
bg-0 (最深):     #09090b    ← 页面背景
bg-1:            #111113    ← 卡片/面板
bg-2:            #1a1a1f    ← 悬浮/弹窗
bg-3:            #252530    ← 输入框/表格行

── 前景/文字 ──
fg-0 (最亮):     #fafafa    ← 标题
fg-1:            #a1a1aa    ← 正文
fg-2:            #71717a    ← 次要文字
fg-3:            #3f3f46    ← 禁用文字

── 边框 ──
border-default:  #27272a
border-hover:    #3f3f46
border-focus:    #f97316    ← 品牌色聚焦

── 品牌色 ──
primary:         #f97316    ← 橙色（温暖·可触）
primary-hover:   #fb923c
primary-muted:   #f97316/15%

── 语义色 ──
success:         #22c55e
warning:         #eab308
error:           #ef4444
info:            #3b82f6

── 特殊 ──
accent-pink:     #f472b6    ← 花羊羊签名色🌸（少量点缀）
```

### 3.2 字体

| 用途 | 字体 | 备注 |
|------|------|------|
| UI / 正文 | Inter | Variable font, 400/500/600/700 |
| 代码 / 数据 | JetBrains Mono | 设备ID、API Key、技术参数 |

**字号梯度：** 12 / 13 / 14（正文）/ 16 / 18 / 20 / 24 / 32 / 48

### 3.3 图标

**Lucide Icons** — 线性风格，1.5px stroke，与 shadcn/ui 原生适配。

补充：3D打印相关自定义图标（打印机、线材、喷头温度等），保持 Lucide 同风格。

### 3.4 组件库清单

基于 shadcn/ui 扩展：

**基础组件：**
- `Button` — primary / secondary / ghost / destructive / outline，3 尺寸
- `Input` / `Textarea` — 含 label、helper text、error state
- `Select` / `Combobox` — 支持搜索的下拉
- `Checkbox` / `Radio` / `Switch`
- `Badge` — status（online/offline/printing/error）+ 语义色
- `Avatar` — 用户头像 + 打印机头像（设备图标）
- `Tooltip` / `Popover`

**布局组件：**
- `Card` — 基础卡片，支持 hover 微抬升
- `Sidebar` — 可折叠导航栏
- `TopBar` — 顶部操作栏
- `Breadcrumb`
- `Tabs` — 页面内切换
- `Separator`

**数据展示：**
- `Table` — 排序、筛选、分页、行展开
- `DataCard` — 统计卡片（数字 + 趋势箭头 + sparkline）
- `EmptyState` — 空状态插画 + CTA
- `Skeleton` — 加载骨架屏

**反馈组件：**
- `Toast` — 右下角通知，自动消失
- `Modal` / `Dialog` — 确认操作、表单弹窗
- `AlertBanner` — 页面顶部告警
- `Progress` — 打印进度条（带百分比 + 剩余时间）

**业务组件：**
- `PrinterStatusBadge` — 实时状态指示（idle/printing/error/offline）
- `PrintProgressBar` — 渐变进度 + 动画脉冲
- `ModuleCard` — 模块商城卡片（缩略图 + 名称 + 价格 + 评分）
- `OrderTimeline` — 订单状态时间线
- `STLPreview` — Three.js 3D 模型预览器
- `NotificationItem` — 通知列表项
- `CommandPalette` — Cmd+K 全局搜索

### 3.5 动效原则

| 原则 | 说明 |
|------|------|
| **克制** | 只在状态变化、反馈、引导注意力时使用动效 |
| **物理感** | 使用 spring 弹簧物理（Framer Motion），不用线性 |
| **快** | 进入 200ms，退出 150ms，悬浮 100ms |
| **有意义** | 每个动效都传达信息（加载→骨架屏，成功→绿色脉冲） |

**标准 spring 配置：**
```ts
const springConfig = {
  gentle: { stiffness: 120, damping: 14 },  // 页面切换、卡片展开
  snappy: { stiffness: 300, damping: 20 },  // 按钮、小元素
  bouncy: { stiffness: 400, damping: 10 },  // 成功反馈、趣味动效
}
```

**必须有动效的场景：**
- 页面路由切换（fade + slide）
- 卡片列表加载（stagger 错开进入）
- 打印进度更新（平滑过渡）
- 通知弹出/消失
- Sidebar 折叠/展开
- Modal 弹出（scale + fade）
- 状态变化（颜色渐变过渡）

---

## 4. 实时功能设计

### 4.1 WebSocket 架构

```
Client (Zustand Store)
  ↕ WebSocket (wss://api.realworldclaw.com/ws)
    ├── channel: printer:{id}    → 打印机实时状态
    ├── channel: order:{id}      → 订单状态更新
    ├── channel: user:{id}       → 个人通知
    └── channel: community       → 社区动态
```

**连接管理：**
- 自动重连（指数退避: 1s → 2s → 4s → 8s → 30s max）
- 心跳检测（30s ping/pong）
- 连接状态指示器（TopBar 右上角小圆点: 🟢连接 🟡重连中 🔴断开）

### 4.2 打印机实时状态

**推送数据模型：**
```ts
interface PrinterStatus {
  id: string
  state: 'idle' | 'printing' | 'paused' | 'error' | 'offline'
  currentJob?: {
    fileName: string
    progress: number        // 0-100
    layer: number
    totalLayers: number
    startedAt: string
    estimatedEnd: string    // ISO timestamp
    nozzleTemp: number
    bedTemp: number
    speed: number           // mm/s
  }
  updatedAt: string
}
```

**打印进度条设计：**
- 渐变色条：primary 橙色 → 完成时渐变为 success 绿色
- 百分比数字（JetBrains Mono）
- 预估剩余时间（"约 1h 23m"）
- 当前层/总层数
- 悬浮显示温度、速度等详细参数
- 进度更新时轻微脉冲动画

### 4.3 通知系统

**通知类型：**

| 类型 | 图标 | 示例 |
|------|------|------|
| order_created | 📦 | "新订单 #1234 已创建" |
| order_matched | 🤝 | "订单 #1234 已匹配 Maker" |
| print_started | 🖨️ | "打印已开始：robot-arm-v2.stl" |
| print_complete | ✅ | "打印完成！用时 3h 42m" |
| print_error | ⚠️ | "打印异常：喷头温度过高" |
| maker_new_order | 💰 | "有新订单可接：距你 2.3km" |
| community | 💬 | "你的设计收到了新评论" |

**通知展示：**
- TopBar bell icon + 未读数量红点
- 点击展开通知面板（右侧抽屉）
- Toast 弹窗（仅高优先级：打印错误、订单匹配）
- 浏览器 Push Notification（用户可在设置中开关）

---

## 5. 技术升级方案

### 5.1 核心依赖升级

```json
{
  "dependencies": {
    "next": "^14.2.0",
    "react": "^18.3.0",
    "@radix-ui/react-*": "latest",
    "class-variance-authority": "^0.7.0",
    "clsx": "^2.1.0",
    "tailwind-merge": "^2.3.0",
    "zustand": "^4.5.0",
    "swr": "^2.2.0",
    "framer-motion": "^11.0.0",
    "lucide-react": "^0.370.0",
    "three": "^0.163.0",
    "@react-three/fiber": "^8.16.0",
    "@react-three/drei": "^9.105.0",
    "cmdk": "^1.0.0",
    "date-fns": "^3.6.0",
    "recharts": "^2.12.0",
    "sonner": "^1.4.0"
  }
}
```

### 5.2 状态管理 — Zustand

```ts
// stores/printer.ts
interface PrinterStore {
  printers: Map<string, PrinterStatus>
  updatePrinter: (id: string, status: Partial<PrinterStatus>) => void
  connectWS: () => void
  disconnectWS: () => void
}

// stores/notification.ts
interface NotificationStore {
  items: Notification[]
  unreadCount: number
  markRead: (id: string) => void
  markAllRead: () => void
}

// stores/ui.ts
interface UIStore {
  sidebarCollapsed: boolean
  theme: 'dark' | 'light'
  commandPaletteOpen: boolean
  toggleSidebar: () => void
  toggleTheme: () => void
}
```

### 5.3 数据获取 — SWR

```ts
// 列表数据用 SWR（自动重验证、缓存、乐观更新）
const { data: orders } = useSWR('/api/orders', fetcher, {
  refreshInterval: 30_000,  // 30s 轮询兜底
})

// 实时数据用 WebSocket → Zustand（不走 SWR）
// 表单提交用 Server Actions 或直接 fetch
```

### 5.4 项目结构

```
src/
├── app/                    # Next.js App Router
│   ├── (auth)/             # 需登录的路由组
│   │   ├── dashboard/
│   │   ├── devices/
│   │   ├── marketplace/
│   │   ├── orders/
│   │   ├── maker/
│   │   ├── studio/
│   │   └── settings/
│   ├── (public)/           # 公开路由
│   │   ├── login/
│   │   └── register/
│   └── layout.tsx
├── components/
│   ├── ui/                 # shadcn/ui 基础组件
│   ├── layout/             # Sidebar, TopBar, AppShell
│   ├── printer/            # 打印机相关业务组件
│   ├── marketplace/        # 商城相关
│   ├── order/              # 订单相关
│   └── shared/             # 跨模块共享
├── stores/                 # Zustand stores
├── hooks/                  # 自定义 hooks
├── lib/                    # 工具函数、API client、WebSocket
├── styles/                 # 全局样式、CSS variables
└── types/                  # TypeScript 类型定义
```

---

## 6. 页面线框图描述

### 6.1 Dashboard 首页 (`/dashboard`)

```
┌─ Sidebar ─┐┌─────────────────────────────────────────┐
│ 🔧 Logo   ││ TopBar: Breadcrumb | Search | 🔔 | 👤   │
│            ││─────────────────────────────────────────│
│ Dashboard◀ ││ "Good evening, Brian" 👋                │
│ 设备       ││                                         │
│ 商城       ││ ┌─ DataCard ──┐ ┌─ DataCard ──┐ ┌─────┐│
│ 订单       ││ │ 🖨️ 设备 3台  │ │ 📦 活动订单 2│ │收入  ││
│ Maker     ││ │ 2 在线 1 离线│ │ ¥128 待结算  │ │¥2.4k││
│ 工作台     ││ └─────────────┘ └─────────────┘ └─────┘│
│            ││                                         │
│ ─────      ││ ┌─ 我的设备（实时） ─────────────────┐  │
│ ⚙️ 设置    ││ │ P2S-01  🟢 Printing  ████░░ 67%   │  │
│ 👤 Brian   ││ │ P2S-02  🟢 Idle                    │  │
│            ││ │ Ender3  🔴 Offline                  │  │
│            ││ └────────────────────────────────────┘  │
│            ││                                         │
│            ││ ┌─ 最近订单 ──────┐ ┌─ 社区动态 ─────┐ │
│            ││ │ #1234 打印中    │ │ 🆕 robot-arm v3│ │
│            ││ │ #1231 已完成    │ │ ❤️ 23 💬 5     │ │
│            ││ │ #1228 已发货    │ │ ...            │ │
│            ││ └────────────────┘ └────────────────┘ │
└────────────┘└─────────────────────────────────────────┘
```

**交互细节：**
- DataCard 点击跳转对应模块
- 设备列表实时更新（WebSocket），进度条平滑动画
- 社区动态无限滚动加载
- 页面加载时卡片 stagger 动画进入

### 6.2 设备管理页 (`/devices`)

```
TopBar: 设备管理 | [+ 添加设备]

┌─ 筛选栏 ─────────────────────────────────┐
│ 状态: [全部 ▾] | 类型: [全部 ▾] | 🔍 搜索 │
└───────────────────────────────────────────┘

┌─ 设备卡片（网格布局，每行 2-3 个）──────────┐
│ ┌───────────────────┐ ┌───────────────────┐ │
│ │ 🖨️ P2S-01          │ │ 🖨️ P2S-02          │ │
│ │ Bambu Lab P2S     │ │ Bambu Lab P2S     │ │
│ │ 🟢 Printing       │ │ 🟢 Idle           │ │
│ │ ████████░░ 67%    │ │                   │ │
│ │ ~1h 23m remaining │ │ 就绪，等待任务     │ │
│ │ 🌡️ 210°C / 60°C   │ │                   │ │
│ │ [查看详情] [暂停]  │ │ [查看详情]        │ │
│ └───────────────────┘ └───────────────────┘ │
└─────────────────────────────────────────────┘
```

**设备详情页 (`/devices/[id]`)：**
- 顶部：设备名称 + 状态 Badge + 操作按钮（暂停/恢复/停止）
- 左栏：实时状态面板（温度曲线图、速度、层数、进度）
- 右栏：当前打印任务详情（文件名、开始时间、预估完成）
- 下方 Tabs：打印历史 | 设备设置 | 维护记录
- 温度图表实时绘制（Recharts，WebSocket推送数据点）

### 6.3 模块商城 (`/marketplace`)

```
TopBar: 模块商城 | 🔍 搜索模块...

┌─ 筛选侧栏 ─┐┌─ 模块网格 ───────────────────────┐
│ 分类         ││ 排序: [热门 ▾] [最新 ▾] [价格 ▾]  │
│ ☐ 机械臂     ││                                    │
│ ☐ 外壳       ││ ┌─ ModuleCard ─┐ ┌─ ModuleCard ─┐ │
│ ☐ 夹爪       ││ │ [缩略图]      │ │ [缩略图]      │ │
│ ☐ 传感器     ││ │ Robot Arm v3 │ │ Gripper Pro   │ │
│ ☐ 底座       ││ │ ⭐ 4.8 (23)   │ │ ⭐ 4.5 (12)   │ │
│              ││ │ ¥15.00       │ │ ¥8.00        │ │
│ 价格范围     ││ │ by @maker01  │ │ by @maker02  │ │
│ [0]──[100]   ││ └──────────────┘ └──────────────┘ │
│              ││                                    │
│ 材料         ││ ┌─ ModuleCard ─┐ ┌─ ModuleCard ─┐ │
│ ☐ PLA        ││ │ ...          │ │ ...          │ │
│ ☐ PETG       ││ └──────────────┘ └──────────────┘ │
│ ☐ ABS        ││                                    │
└──────────────┘│ [加载更多...]                       │
                └────────────────────────────────────┘
```

**模块详情页 (`/marketplace/[id]`)：**
- 左侧：3D 预览窗口（Three.js，可旋转/缩放/全屏）
- 右侧：名称、描述、价格、Maker 信息、评分
- 参数表：材料、尺寸、打印时间、重量
- 操作按钮：[立即下单] [加入收藏] [分享]
- 下方 Tabs：详细说明 | 评价 | 相关模块

### 6.4 订单管理 (`/orders`)

```
TopBar: 订单管理 | [+ 创建订单]

Tabs: [全部] [进行中] [已完成] [已取消]

┌─ 订单表格 ─────────────────────────────────────┐
│ 订单号   模块        状态      Maker   金额  时间 │
│ #1234   Robot Arm   🟡打印中  maker01  ¥15  2h前 │
│ #1231   Gripper     🟢已完成  maker02  ¥8   1d前 │
│ #1228   Base Plate  🔵已发货  maker01  ¥12  3d前 │
│ #1225   Sensor Mt   🟢已完成  maker03  ¥6   5d前 │
└─────────────────────────────────────────────────┘

分页: < 1 2 3 ... 12 >
```

**订单详情页 (`/orders/[id]`)：**
- 顶部：订单号 + 状态 Badge
- OrderTimeline 时间线组件：
  - 🔵 已创建 → 🔵 已匹配 → 🟡 打印中（当前）→ ⚪ 质检 → ⚪ 发货 → ⚪ 完成
- 打印实时状态（如果正在打印，显示进度条 + 摄像头截图）
- 模块信息卡片 + Maker 信息卡片
- 操作：[联系 Maker] [取消订单] [评价]

**创建订单 (`/orders/new`)：**
- Step 1: 选择模块（从商城/收藏中选）
- Step 2: 配置参数（材料颜色、数量、备注）
- Step 3: 确认下单（价格预览、预计时间）
- 多步表单，带进度指示器，每步可返回修改

### 6.5 Maker 中心 (`/maker`)

```
TopBar: Maker 中心

┌─ 未注册状态 ─────────────────────────────────┐
│                                               │
│   🔧 成为 Maker，用你的打印机赚取收入          │
│                                               │
│   ✅ 接受社区订单    ✅ 设定自己的价格          │
│   ✅ 灵活安排时间    ✅ 加入 Maker 社区         │
│                                               │
│              [立即注册成为 Maker]               │
│                                               │
└───────────────────────────────────────────────┘

┌─ 已注册状态 ─────────────────────────────────┐
│                                               │
│ ┌─ DataCard ─┐ ┌─ DataCard ─┐ ┌─ DataCard ─┐ │
│ │ 💰 本月收入 │ │ 📦 已完成   │ │ ⭐ 评分     │ │
│ │ ¥2,480     │ │ 34 单      │ │ 4.9        │ │
│ │ ↑12%       │ │ ↑5 vs上月  │ │ (128评价)  │ │
│ └────────────┘ └────────────┘ └────────────┘ │
│                                               │
│ Tabs: [待接单] [进行中] [历史] [收入明细]      │
│                                               │
│ ┌─ 可接订单 ────────────────────────────────┐ │
│ │ #1240 Robot Arm v3 | PLA白 | ¥15 | 2.3km │ │
│ │     [接单] [跳过]                          │ │
│ │ #1241 Gripper | PETG黑 | ¥8 | 1.1km      │ │
│ │     [接单] [跳过]                          │ │
│ └────────────────────────────────────────────┘ │
│                                               │
│ 能力标签: [PLA] [PETG] [ABS] [+ 添加]        │
│ 设备: P2S-01 ✅ | P2S-02 ✅                   │
└───────────────────────────────────────────────┘
```

### 6.6 设计工作台 (`/studio`)

```
TopBar: 设计工作台 | [+ 上传设计]

┌─ 我的设计（网格）──────────────────────────────┐
│ ┌──────────┐ ┌──────────┐ ┌──────────┐        │
│ │ [3D缩略] │ │ [3D缩略] │ │ [3D缩略] │        │
│ │ Arm v3   │ │ Base v2  │ │ Gear Set │        │
│ │ 草稿     │ │ 已发布   │ │ 已发布   │        │
│ │ 3天前编辑│ │ ❤️ 23     │ │ ❤️ 45    │        │
│ └──────────┘ └──────────┘ └──────────┘        │
│                                                │
│ ┌──────────┐                                   │
│ │    +     │                                   │
│ │ 上传新设计│                                   │
│ └──────────┘                                   │
└────────────────────────────────────────────────┘
```

**设计编辑页 (`/studio/[id]`)：**
- 左侧（60%宽）：Three.js 3D 预览，支持旋转、缩放、线框切换、截面视图
- 右侧（40%宽）：
  - 设计名称（可编辑）
  - 描述（Markdown 编辑器）
  - 参数：材料建议、打印设置、尺寸
  - 标签
  - 状态：草稿 / 已发布
  - [保存草稿] [发布到社区] [下载 STL]

**上传流程 (`/studio/upload`)：**
- 拖拽区域上传 STL/3MF 文件
- 上传后自动渲染 3D 预览
- 自动检测尺寸、估算打印时间
- 填写基本信息后保存为草稿

### 6.7 设置页 (`/settings`)

```
TopBar: 设置

Sidebar Tabs (垂直):
├── 个人信息
├── API Keys
├── 通知偏好
├── 外观
└── 账号安全

┌─ 个人信息 ─────────────────────────────────┐
│ 头像 [上传]                                 │
│ 用户名: brian                               │
│ 邮箱: brian@example.com                     │
│ 简介: ...                                   │
│ 地区: 上海                                  │
│                         [保存修改]          │
└─────────────────────────────────────────────┘

┌─ API Keys ─────────────────────────────────┐
│ [+ 创建新 Key]                              │
│                                             │
│ rwc_live_****3f2a  创建于 2026-01-15  [删除]│
│ rwc_test_****8b1c  创建于 2025-12-01  [删除]│
│                                             │
│ Key 以 JetBrains Mono 显示，点击复制        │
└─────────────────────────────────────────────┘

┌─ 通知偏好 ─────────────────────────────────┐
│ 订单状态更新    [邮件 ✅] [推送 ✅] [站内 ✅]│
│ 打印完成        [邮件 ☐] [推送 ✅] [站内 ✅] │
│ 打印异常        [邮件 ✅] [推送 ✅] [站内 ✅]│
│ 社区互动        [邮件 ☐] [推送 ☐] [站内 ✅] │
│ 新订单可接(Maker)[邮件 ☐] [推送 ✅] [站内 ✅]│
└─────────────────────────────────────────────┘
```

---

## 7. 实施路线图

### Phase 1 — 基础设施（1-2 周）
- [ ] 初始化 shadcn/ui + Tailwind CSS 变量
- [ ] 搭建 AppShell（Sidebar + TopBar + 路由布局）
- [ ] 配置 Zustand stores（ui / auth）
- [ ] 配置 SWR + API client
- [ ] 暗色主题 CSS 变量
- [ ] Framer Motion 基础配置

### Phase 2 — 核心页面（2-3 周）
- [ ] Dashboard 首页
- [ ] 设备管理 + WebSocket 实时状态
- [ ] 模块商城 + 搜索筛选
- [ ] 订单管理 + 创建流程

### Phase 3 — 高级功能（2-3 周）
- [ ] 3D 预览（Three.js）
- [ ] Maker 中心
- [ ] 设计工作台
- [ ] 通知系统
- [ ] Cmd+K 全局搜索

### Phase 4 — 打磨（1-2 周）
- [ ] 动效优化
- [ ] 响应式适配
- [ ] 空状态 / 错误状态设计
- [ ] 性能优化（懒加载、代码拆分）
- [ ] 亮色主题

---

> 🌸 花羊羊说：
>
> 好的产品不是功能的堆砌，而是每个像素都在为用户思考。
> 暗色主题不是为了酷，是因为 Maker 们经常在深夜打印。
> 进度条的每一帧动画，都是对等待者的温柔。
>
> — 温暖 · 克制 · 可触 · 有趣 🌸
